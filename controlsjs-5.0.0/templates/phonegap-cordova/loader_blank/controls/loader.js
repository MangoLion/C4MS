function ngLoadApplication() {
  document.body.innerHTML=
  '<div style="text-align:center;"><h3>Template requirement</h3>Replace files in "controls/" folder with files from "build/release/{version}/" or change "controls/" references in index.html.</div>';
}
